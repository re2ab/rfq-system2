<?php

/**
 * تنظیمات موتور اسناد (M7). طبق سند معماری، هدف cPanel اشتراکی است — یعنی
 * درایور تبدیل PDF یک تصمیم زیرساختی است که به دسترسی exec/shell_exec روی
 * همان اکانت خاص بستگی دارد (سند معماری آن را OQ-5 نام‌گذاری کرده)، نه یک
 * چیزی که کد بتواند حدس بزند. مقدار پیش‌فرض 'auto' یعنی سیستم خودش
 * LibreOffice را امتحان می‌کند و اگر نبود، بی‌صدا به «فقط دانلود Word/Excel»
 * برمی‌گردد — هیچ‌وقت با خطای مبهم برای کاربر نهایی متوقف نمی‌شود.
 */
return [
    // auto | libreoffice | none
    'pdf_driver' => env('PDF_DRIVER', 'auto'),
];
