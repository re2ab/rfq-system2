# RFQ-Core — بازطراحی رابط کاربری (Graphite + Copper)

## چه چیزی تغییر کرده
1. **`public/css/rfq-ui.css`** — دیزاین‌سیستم کامل (توکن‌های رنگ/فاصله/سایه/رادیوس، تایپوگرافی،
   نرمال‌سازی فرم‌ها و جدول‌ها، حالت تیره، RTL/LTR، اسکلتون‌ها، صفحه ورود).
   پالت: گرافیت `#16191f` + مسی `#b8703c`.
2. **`resources/views/layouts/app.blade.php`** — پوسته جدید: سایدبار جمع‌شونده گرافیتی،
   تاپ‌بار شیشه‌ای با جست‌وجوی سراسری (کلید `/`)، منوی کاربر، ناوبری موبایل.
3. **`resources/views/components/*`** — کتابخانه کامپوننت‌ها بازنویسی و گسترش یافت
   (btn, badge, card, alert, empty, filter-bar, page-header, stat, skeleton, data-table,
   modal, drawer, dropdown, tabs, avatar, timeline-item, kanban-card).
4. **`resources/views/auth/login.blade.php`** — ورود دو ستونه لوکس.
5. **پاکسازی سراسری**: رنگ‌های هارد‌کد در ~۲۰ ویو به توکن تبدیل شد، پالت نمودارها
   (Chart.js) و رنگ پیش‌فرض برند در `SettingsController`/`TagController`/سیدر قالب پیشنهاد
   به مسی/گرافیت به‌روزرسانی شد.

## نصب
1. فایل‌ها را روی پروژه کپی کنید (فقط `public/css`, `resources/views`, و سه فایل PHP بالا).
2. کش ویو را پاک کنید: `php artisan view:clear`
3. اگر `primary_color` قبلاً در تنظیمات ذخیره شده، از مسیر تنظیمات ← ظاهر مقدار `#b8703c` را ثبت کنید.

## نکات
- هیچ منطق بک‌اندی (کنترلر، مدل، مایگریشن، روت) تغییر نکرده؛ فقط مقادیر پیش‌فرض رنگ.
- تمام ویوها بدون بازنویسی دستی از نرمال‌سازی سراسری فرم/جدول بهره می‌برند.
- برای حالت تیره، کلاس `dark` روی `<html>` کافی است (سوییچ در منوی کاربر).
