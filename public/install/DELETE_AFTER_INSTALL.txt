پس از نصب موفق سیستم RFQ-Core، این پوشه (public/install) را کامل حذف کنید.

دلیل: ویزارد می‌تواند اطلاعات دیتابیس را بپذیرد و دستورات artisan را اجرا کند و نباید در دسترس اینترنت بماند.

دستور:
  rm -rf public/install

قفل نصب در این مسیر ایجاد می‌شود:
  storage/app/install.lock
